else if(when_free[2*i+2].val==when_free[i].val &&  when_free[i].ind>when_free[2*i+2].ind ){
      //   node temp=when_free[2*i+2];
      //   when_free[2*i+2]=when_free[i];
      //   when_free[i]=temp;
      //   i=2*i+2;
      // }
      
      // else if(when_free[2*i+1].val==when_free[i].val &&  when_free[i].ind>when_free[2*i+1].ind ){
      //   node temp=when_free[2*i+1];
      //   when_free[2*i+1]=when_free[i];
      //   when_free[i]=temp;
      //   i=2*i+1;
      // }